<Project Sdk="Microsoft.NET.Sdk">

  <!-- AIF.Trust - Zero Trust with MPAI-PTF (Phase 13, M3223): the canonical form
       and the signatures of PTF objects, the identities, credentials and evidence of
       AIM Instances, and the Trust Protocol. Depends on nothing but the platform's
       cryptography, so that a Controller, a host and an AIM can all use it. -->

  <PropertyGroup>
    <TargetFramework>net10.0</TargetFramework>
    <ImplicitUsings>enable</ImplicitUsings>
    <Nullable>enable</Nullable>
  </PropertyGroup>

</Project>
